# 请在 app.py 中的 app.register_blueprint(delete_bp, url_prefix='/delete') 之后添加以下代码：

# 注册密码管理路由蓝图
from routes.password_routes import password_bp
app.register_blueprint(password_bp, url_prefix='/password')
